info:
  name: obt_token
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/api/token/
  body:
    type: json
    data: |-
      {
        "username":"admin",
        "password":"asdqwe123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
