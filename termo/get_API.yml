info:
  name: get_API
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/bloques_todos/
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
